## 1. Implementation

- [x] 1.1 在 `globals.css` 中添加 CSS keyframes 动画定义
  - `@keyframes cyber-grid-scroll` - 网格滚动动画
  - `@keyframes scan-beam` - 扫描光束动画
- [x] 1.2 添加 `.cyber-grid-anim` 和 `.cyber-scan-line` CSS 类
- [x] 1.3 修改 `TradingPhaseChart` 组件，为背景网格添加动画类
- [x] 1.4 添加扫描光束 DOM 元素
- [x] 1.5 确保动画不影响图表交互 (pointer-events: none)
- [x] 1.6 验证动画性能，使用 `will-change` 优化

